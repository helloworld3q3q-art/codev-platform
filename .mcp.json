{
  "_comment": "platform ownership inversion 2026-05-28: launcher 现归 codev-platform 自有 (tools/). venv 在仓根 .venv. multi-tenant daemon 按 project_id 路由到正确 collection. 跨机 portable.",
  "mcpServers": {
    "codegraph": {
      "type": "stdio",
      "command": "codegraph",
      "args": ["serve", "--mcp"]
    },
    "platform-docs": {
      "type": "stdio",
      "command": "cmd",
      "args": ["/c", "tools\\chroma\\platform-docs-mcp.cmd"],
      "timeout": 90000
    },
    "cross-link": {
      "type": "stdio",
      "command": "cmd",
      "args": ["/c", "tools\\cross_link\\cross-link-mcp.cmd"],
      "timeout": 30000
    }
  }
}
