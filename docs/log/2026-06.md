# Dev Evolution Log — 2026-06

> 工具栈 / 协作流程的月度决策聚合(工具栈侧)。业务事故复盘走 `docs/operations/`;本文件只记平台本体演进。

---

## 2026-06-01 — 多组织服务器加固 + 2026-06-01 审计整改(大 session)

把 codev-platform 从"单人 editable 能跑"系统性推进到"多组织/多人/多项目可安全共享 + 可在干净 venv 交付 + 可演示"。全程在 **WSL 真值源仓 `~/work/codev-platform`** 改、推 Gitea `dev`,Windows 克隆同步。测试 **165 → 345 passed**。详细路线 + 逐轮 commit 见 `docs/plans/roadmap-2026-05-31/multi-org-server-hardening-2026-05-31.md §六`。

### 1. 多组织加固 A 组(安全)全闭环

**做了什么**:P1 项目 ACL(`core/acl.py:can_access` 单一真值源 + Identity 项目白名单 + 五执行点 + dev/prod 双模式,`25101a9`)→ P2 token 模式启用 + 越权审计日志(`dcf16a1`/`59edc48`)→ P3 TLS 反代 + health 拆分(`/healthz` public / `/platform/status` 鉴权,`66c1c15`)→ A4 速率限制(`core/ratelimit.py` 滑窗,按身份/IP,默认关,`49eb89d`)。A1-A5 全到位。

**为什么**:部署形态是"多团队(多组织)多人多项目,一上来就细到人/项目"(用户拍板)。模型 C(org 归属 + per-token 白名单)与 memory 权限模型 §3.4 同源。

### 2. memory M5 org/team RBAC + 真 PG 验证

**做了什么**:`core/rbac.py` 纯逻辑(`Membership`/`compute_visible_scopes`/`role_allows`/`memory_scope_decision`,零 psycopg)+ `agent/rbac_store_pg.py` 七表 DDL + `ops/org.py` D13 管理 CLI + wire recall/memory(无 store 优雅回退,`0069629`)。**在 WSL 本地新装的 PostgreSQL** 上 `verify_rbac_pg.py` + `verify_memory_pg.py` 全绿(七表幂等 / 跨 org 硬隔离 / 会话持久化等)。

**为什么 + 决策**:memory 数据库**独立于 Windows PG**(用户要"区分":WSL server 用 `localhost:5432/codev_platform_memory`,Windows 若 standalone 用 `_win` 库;各机 `memory.pg_dsn` 各管各的,机器相关不进 git)。psycopg 装入平台 `.venv`。

### 3. 2026-06-01 三轮审计整改(9/10)

**做了什么**:对照 `docs/audits/codev-platform-audit-report-2026-06-01.md` 整改。阶段1 运行态(`8397dde`):#1 chroma 重依赖 lazy 化 + 去 import 期 `sys.exit`(修唯一失败测试根因)、#3 healthz→health 探针兼容旧 daemon、#4 chroma per-project reload stamp、#5 health 用 `cross_link_db_path`、#6 `deployment.mode=prod` 非 token fail-fast、#7 `config doctor --redact`、#10 非法 project_id 返 400。边缘项(`8a3c878`):B8 时钟重同步 timer / E16 agent 常驻 unit / F18 codegraph 自动 ensure-link / C12 集中日志。

**关键**:adversarial 审计连续多轮各抓出一个真 bug(SSE 回退顺序 / runbook 端口三处不一致 / 备份 manifest 密码泄漏),全修。整改明细 `docs/audits/audit-0601-remediation-status.md`。

**状态**:9/10 整改;#8 Docker 闭环 + 阶段3 商业化(二进制/license/插件化)用户拍板押后。

### 4. #2 relocate —— wheel 可交付

**做了什么**:`rules/` `skills/` 真值源从仓库根 `git mv` 进 `codev_platform/resources/`,CLI 经 `importlib.resources` 定位 + `pyproject` package-data,使**普通 `pip install`(非 editable)的 wheel 也带得走** → 客户机 `sync-rules`/`sync-skills` 可用(`01ab0c5`)。`scripts/verify_clean_install.sh` 真 build wheel + 装空 venv 实测 PASS(`5a2211a`)。`config.example.json` 代码不读,留仓根。

**为什么**:审计 #2 指出 wheel 缺资源,普通安装后 sync 失败。relocate 改变了真值源编辑路径(改 `codev_platform/resources/rules/<name>.md`),CLAUDE.md 已同步。

### 5. GPU 参数配置化

**做了什么**:reranker `.cuda()` 写死 → `.to(device)`(支持 `cuda:N`/`cpu`,修多卡 pin 隐性 bug);`models.reranker_dtype`(auto/fp16/bf16/fp32)+ `models.embed_batch_size` 配置化(`d7e6e20`)。**换显卡(本地小卡 ↔ 服务器大卡)只改 config**。

### 6. 阶段2 可演示

**做了什么**:`demo/`(AcmeShop 虚构文档,无真实仓也能试检索)+ `docs/QUICKSTART.md`(install→config→index→serve→test query)+ `docs/playground/index.html`(只读 Web 问答,需 `agent serve`)(`5a2211a`)。

### 协作方式备注

本 session 大量使用**多 agent workflow**(fix→test→adversarial audit 三段),每批落地后独立复核 diff + 自跑测试 + 提交推送。审计 agent 的对抗式核查多次拦下纯函数测试照不到的入口/集成缺口,价值显著。
