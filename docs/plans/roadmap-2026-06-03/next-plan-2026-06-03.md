# Next Plan — roadmap-2026-06-03 迭代 · 图谱收敛 → 影响分析 → 平台收尾

> 起点:2026-06-03(承上一迭代 `../roadmap-2026-06-01/`)
> 战略重心:**把统一图谱 store 做成连通的全链路单一真值源,解锁"影响分析报告"核心商业价值**;
> 同步收尾 web-backend / Agent Memory 起步 / 技术债。
> 生命周期纪律:`weekly-iteration-cadence.md`;验证:`verification-checklist.md`;commit 无 AI 痕迹。

---

## 一、上一迭代盘点(继承基线)

- roadmap-2026-06-01 粗略 **已做 ~25 / 未做 ~30**。
- **已闭环(✅,不重做)**:血缘专题 P1–P4(`../roadmap-2026-06-01/unified-graph-lineage-2026-06-03/`)—— store 单一来源、跨层链路并入统一图谱、cross-link 死代码清理、审计 688 passed、真实库 0 dangling/orphan/归属违规。web-backend Phase 0–6 骨架、pluginized Phase 0–4、refactor A 主体+B 主面。
- **关键洞察(本迭代地基)**:全链路**连通性**现 `cross-link > store`(store 端点出边=0,端点↔表断,缺方法级桥);**store + codegraph 桥接可取代 cross-link 且更准更全**(去重+AST+多上游),并退役 `build_index.py`,顺手解锁影响分析。
- **本会话补做闭环(2026-06-03,✅)**:[`deep-audit-2026-06-03`](../../audits/deep-audit-2026-06-03.md) **12 项全闭环** + 复核文档 [`deep-audit-2026-06-03-review.md`](../../audits/deep-audit-2026-06-03-review.md) —— P1 projects 两级 RBAC + 双轨 session 鉴权 / logout 撤 access / register 原子写;P2 reindex runner timeout / graph project_id 隔离 / **D1 SQLAlchemy Core 迁移(见下)** / **前端 TS 4.9→5.4(tsc 36→0,删 8 死模板)** / PG fail-fast;P3 日志迁 data_root / 移 @ts-nocheck / broad except 可观测。对抗审计修 1 HIGH(search_recall 读写路径不一致)。提交 `b67d7f2…c4d632b`,**740 passed / tsc 0**。

---

## 二、本迭代核心目标(5)

1. **store 成连通全链路单一真值源**(补 endpoint→function 桥),具备退役 cross-link 条件。
2. **解锁影响分析**:Agent 工具 + 报告("改一处 → 跨层影响清单")—— README 核心卖点。
3. **web-backend 收尾**(Phase 7 接入 + Phase 8 契约测试 + 退役 Java codegraph-api)。
4. **Agent Memory 起步**(M1 任务记忆闭环)。
5. **技术债 + 质量护栏 + Demo/POC**。

---

## 三、四条轨道(优先级 + 依赖)

### Track A — 图谱收敛 + 影响分析(spearhead,最高优先)

| 步 | 任务 | 关键文件 | 验证 |
|---|---|---|---|
| ✅ **A1** | ingest 加 **codegraph 桥**:用 codegraph 调用图把 `backend_endpoint(meta.handler) → backend_function(碰表)` 边物化进 store,store 自成连通全链路 (`48cb0e1`,openclaw 155 边) | `graph/ingest.py` + 新 `graph/bridge_codegraph.py` | openclaw 端点出边>0;端到端可追 前端→端点→方法→表 |
| **A2** | **parity 对账工具**:store(连通后)vs cross_layer,核 endpoint/table/连通数 ≥ | `tools/audit_graph_parity.py` | store 覆盖 ≥ cross_layer |
| **A3** | **退役 cross-link 生产**:parity 达标 → 停/降频 reindex `--cross-link` + `build_index.py` | `reindex/runners.py` / `ops/reindex.py` | cross_layer 不再每 push 重建,少一路 |
| ✅ **A4** | **影响分析引擎 + Agent 工具**:`find_impact` / `find_table_usage` / `find_page_dependencies` / `find_api_callers`(`a5aa587` 引擎 + `3068af5` agent 工具注册) | `agent/tools/impact.py` + `graph/impact.py` | 给表/接口返回完整跨层影响 |
| ✅ **A5** | **影响报告**:`generate_impact_report`(改动 → 跨层影响+风险),web `/reports/impact` 等 4 路由 (`0c5be0a`,已上线 18088) | `graph/impact.py` + `web/routes/reports.py` | 一份可读影响报告 |
| **A6** | **cross-link MCP 迁移**:`find_endpoint_link`/`find_table_refs` 改走 store(留兼容期) | `cross_link/server.py` | MCP 不依赖 cross_layer |

> 依赖:A4/A5←A1(连通图);A3←A2(parity);A6←A1+A4。**关键路径 A1→A2→A4→A5**。

### Track B — web-backend 收尾(Phase 7/8)

- **B1** Phase 7 路由:`agent.py / memory.py / reports.py / audit.py` + integrations(chroma/agent/reindex client)。
- **B2** Reports 模块(**依赖 A4/A5**)。
- **B3** Phase 8 契约测试(operationId 唯一 / OpenAPI 快照)。
- **B4** 退役 Java codegraph-api(:18082)切流下线。
- **B5** `codev-platform web serve` CLI 子命令注册。

### Track C — Agent Memory 平台化(先起 M1)

- **C1** M1 任务记忆闭环(`task_id` / `task_memory` / `task_state`)。
- **C2** M2 Context Engineering(budget/分组)。M3–M7 排后,本迭代不铺开。

### Track D — 技术债 + 质量 + 交付(并行)

- **D1** ✅ **完成(2026-06-03,`0e6f220` + Alembic `c25066f`)** SQLAlchemy 迁移:account_store_pg + rbac_store_pg(7 表)psycopg 裸 SQL → SQLAlchemy Core,`_SCHEMA` 收口 `web/db/tables.py`,方言感知 upsert,sqlite 内存引擎集成测试覆盖 fetch_membership,Alembic baseline 与 tables.py **0-diff**;图谱 SQLite 只读 raw SQL 未动。
- **D2** `tests/test_file_size_budget.py` 静态断言(防大文件复发)。
- **D3** agent FastAPI 路由补 `code` 字段 + 停 `str(e)` 外泄。
- **D4** Demo 项目(CRM 多栈)+ 私有化 POC 包(pluginized Phase 7/8)。
- **D5** `_java_scan` 语言基座抽象;MyBatis-Plus 按调用点细分读/写。

---

## 四、排期(依赖序)

```
第一刀  A1 桥接 → A2 parity → (达标)A3 退役准备      ← 图谱连通,本迭代地基
核心价值 A4 影响引擎 → A5 影响报告                    ← README 核心卖点
接入    B1 web Phase7 → B2 Reports(吃 A4/A5)
平台    C1 memory M1  //  D1 SQLAlchemy / D2-D3 质量  并行
收尾    A6 MCP 迁移 / B3-B5 / D4-D5
```

关键路径:**A1 → A2 → A4 → A5 → B2**;其余并行。

---

## 五、成功指标

- store 连通:`backend_endpoint` 出边 > 0;任取表/接口端到端可追(上游采集→表→后端方法→端点→前端)。
- parity:store 覆盖 ≥ cross_layer(endpoint/table/前后端连通数)。
- 影响分析:给定改动,Agent 返回完整跨层影响 + 报告。
- 退役:cross_layer / build_index.py 不再每 push 重建。
- 测试基线不降(≥ **688 passed**);新功能均带测试。

---

## 六、禁止做 / 风险红线

- ❌ **parity 未达标前不删 cross_layer**(openclaw 全链路目前唯一连通源)。
- ❌ **不把方法级节点灌进 store**(架构级保持;桥接只物化 endpoint→function 一条边,不搬 4000+ 方法)。
- ⚠️ 桥接 join 按 `文件+限定名`,设调用链深度上限防爆炸;命中不了降级(不挂边不报错)。
- ⚠️ 写侧串行(ingest/reindex/store 重建单实例);删插件/改 schema 后须重建 store(orphan 坑)。
- ⚠️ SQLAlchemy 迁移:已发布 Alembic baseline 不回改,只 forward;store SQLite 只读路径不动。

---

## 七、关联

- 上一迭代:`../roadmap-2026-06-01/`(web-backend / agent-memory / pluginized / refactor / sqlalchemy 详细 plan 是各 Track 的实施真值源,本计划只排序+追踪)。
- 前序完成:`../roadmap-2026-06-01/unified-graph-lineage-2026-06-03/`(血缘专题)。
- 纪律:`weekly-iteration-cadence.md` / `verification-checklist.md` / `commit-pr-conventions.md`。
