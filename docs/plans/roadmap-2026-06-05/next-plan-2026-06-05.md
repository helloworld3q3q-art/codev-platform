# Next Plan — roadmap-2026-06-05 · 综合理解层(从"查依赖"到"懂系统")

> **v2(2026-06-04 四专家会诊后修订)** —— 架构/产品/AI/实施四视角收敛: ① ROI 重排(收尾先于
> 新特性)② Track A 狠切只读 MVP ③ 软/硬节点物理隔离 ④ grounding-first 抗幻觉。原 v1 见 git 历史。
>
> **主题**: 在已有技术血缘之上加 **LLM 综合分析层**,但**先收尾真缺口、再上新特性**;综合理解层
> 本轮只交"业务域映射只读 MVP"(可证伪 ≥70%),tour/persona/web-ui 推下轮专轮。
> **定位(产品专家锤死)**: UA = 教人读懂任意代码(广浅, human-first);**codev = 让 agent + 团队
> 在多项目间共享对自有系统的精确理解(深专, AI-first)**。不跟 UA 卷 tour/persona 视觉。
>
> **起点**: 2026-06-05 | **承接**: [`../roadmap-2026-06-03/`](../roadmap-2026-06-03/)(主线已闭环)
> **触发**: 对比 [Understand-Anything](https://github.com/Lum1104/Understand-Anything) —— 借其
> domain/tour 高层分析, 用 codev 独有 Agent+Memory 底座实现;❌ 不抄 tree-sitter(和 codegraph 重叠)。

---

## 一、上一迭代继承(2026-06-04 代码级核实)

Memory **M0–M4 全闭环**、影响分析三层全在、18082 退役、agent-chat-sessions A/B/C 做完。真未做仅:
M5 压测 / M6 Connector / M7 多模态(缓) · M3 向量召回(B1) · M4 cron(B2) · Store project_id 列(C1) ·
endpoint→表 DI(D3) · 前端依赖图 backlog(D1/D2)。

---

## 二、核心目标(5)

1. **先收尾真缺口**(C1 多租户安全 + B1 向量召回)——价值确定、可断言,**优先于**新特性。
2. **综合理解层 A1 只读 MVP**: 业务域映射,**准确率 ≥70% 为验收**(可证伪,低则止损)。
3. **多租户加固**: Store project_id 列化(C1)+ 压测摸底(C2)。
4. **A2 架构分层 / A3 tour / A4 web-ui+persona 推下轮专轮**(本轮不做,避免半成品)。
5. **质量纪律**: 每项配真实验证;LLM 产物**代码侧硬校验**(grounding,非 prompt 劝)。

---

## 三、轨道(⚠️ ROI 重排: C1 → B1 → A-MVP → C2/B2/D)

> 四专家共识: 原 v1 把 A(新特性、价值未验)当 spearhead 是**排序倒了**。C1 是真安全隐患且
> **是 A 的硬依赖**(A 写软节点需隔离列,否则返工);B1 接缝已留、价值确定。故收尾先行。

### Track C1 — Store project_id 列化(最先, A 的前置)

- `edges`/`findings`/`evidences` 加 project_id 列(现仅 plugin 隔离, 共库理论串)。forward migration
  + 查询带 project_id。**验证**: 多项目共库 0 串(测试断言)。
- ⚠️ **硬依赖**: A1 软节点也写 edges,必须 C1 先列化,否则软边先天缺隔离 → 返工。

### Track B1 — M3 向量语义召回

- `recall_backend=vector` 接 chroma,实现 `VectorRecallService`(接缝已留)。
- **AI 专家关键约束**: 向量**只替换排序层**——`visible_scopes + resolve_conflicts + redline-first`
  全保留前置,向量候选走 chroma RRF(关键词∪向量),否则语义排序会把 redline 排下去。
- **独立 collection** `<pid>__agent_memory`(与 platform_docs 文档向量隔离)+ **写时增量 embed**(低频写, 不全量重建)。
- **验证**: 语义近似召回 > 纯关键词;redline 仍置顶。

### Track A1 — 业务域映射(只读 MVP,本轮唯一综合理解步)

在统一图谱 + Agent 底座之上, LLM 给 endpoint 链路归业务域。**不碰 plugins**(护城河)。

**架构定位(架构专家)**: analyzers 是 ingest 的 **second post-pass**(在 `_calls_pass` 之后),
独立 `graph/analyzers/base.py`(协议族+registry, 与 call_resolvers 平级),**不混进 plugins**。

**三道护栏(缺一即 PPT)**:
| # | 护栏 | 怎么做 |
|---|---|---|
| ① **grounding** | LLM 当 labeler 不当 extractor | closed-world 喂 `generate_impact_report` 输出(endpoint→表清单), "只能从清单 token 归纳, 不许引入清单外实体";**越界节点代码 reject 重试**(硬约束>prompt) |
| ② **软/硬隔离** | 保护"查依赖"护城河 | 独立 `NodeKind.BUSINESS_DOMAIN` + `EdgeKind.BELONGS_TO_DOMAIN`;**confidence 强制 <1.0**;软节点 meta 带 `derived_by=llm/model/prompt_version/source_node_ids`;**impact BFS 默认过滤软边**(查依赖不串, 查理解才放开);落库前过**确定性 referential-integrity 校验**(软边端点必须指向真实硬节点, 悬空即丢) |
| ③ **纠错回路** | PPT vs 真功能分水岭(产品专家) | LLM 必错 → ownership 支持**一键纠正 + 持久化复用**(改过的 domain 不被下次 LLM 覆盖) |

**成本控制(AI+实施专家)**:
- **cluster-batch**: 先确定性连通分量/Louvain, 一个 LLM call 标一个域(O(域数) 不是 O(节点数))。
- **node-subgraph fingerprint 缓存**: key = `hash(子图序列化 + model + prompt_version)`——LLM 非确定性,
  换模型/改 prompt 必失效(frontend_deps 无此问题, LLM 有)。失效**按 flow/链路粒度**(domain 跨节点, 非单文件)。
- **token 预算 + 子图采样**: 大仓(openclaw 4427 chunks)禁全图喂, 先采样护栏。
- **analyzer 按难度配 model**: domain 命名用本地小模型(Qwen)够, 走 brain 多 provider 路由。

**understand() 工具(架构 O1)**: 只读组合视图(BFS 硬边 join 软节点), **不在工具内触发 LLM**——
LLM 只在 ingest 跑一次落库, 查询零延迟可复现。

**验收(实施专家, 可证伪)**: 选 1 仓(openclaw-stock)、10 个 endpoint, **人工核对 domain 准确率 ≥70%**。
低于 70% = 方向证伪, 及时止损。

### Track C2/B2/D — 收尾(并行/末段)

- **C2 压测摸底(M5 起步)**: seed 脚本造 N 万条 memory + chroma 并发 → 测 recall/写入 **P95**;
  **数据固化复用给 B1 当 benchmark**(一份数据两用)。同时测 A1 全仓首次 understand 的 LLM 总耗时+成本+缓存命中率。
- **B2 M4 cron**: compress_topic/archive_expired 接 systemd timer 或 reindex worker 定时。
- **D1** is_page Next 验证 / **D2** vue 仓 scl-www-10 登记 / **D3** endpoint→表 DI 精确版评估。

---

## 四、排期(依赖序, 收尾先行)

| 时段 | 主线 | 并行 |
|---|---|---|
| 起点–周一 | **C1 Store project_id 列化**(A 的前置) | — |
| 周二–周三 | **B1 M3 向量召回** | analyzers 框架骨架(base+缓存) |
| 周四–周五 | **A1 业务域 MVP**(三护栏 + ≥70% 验收) | C2 压测造数 |
| 收尾 | A1 准确率核对 + 止损判断 | B2 cron / D1–D3 |

> A2/A3/A4(架构分层/tour/web-ui/persona)**整体推迟下一轮专轮**——本轮证明 A1 方向(≥70%)再投。

---

## 五、成功指标

- **C1**: Store 多项目共库 **0 串**(project_id 列化 + 测试断言)。
- **B1**: 向量召回上线, 语义近似可召回, **redline 仍置顶**(不变量不破)。
- **A1**: 10 endpoint 业务域**准确率 ≥70%**(可证伪);软节点 confidence<1.0 + impact 默认不串。
- **质量**: 每项真实验证(端到端/计时/Bash 直读), **LLM 产物代码侧硬校验**(越界 reject)。

---

## 六、不做清单(纪律 — 本会话 + 会诊沉淀)

- ❌ **A2/A3/A4 本轮不做**(架构分层/tour/web-ui/persona)——推下轮专轮, 先证 A1 方向(实施+产品专家)。
- ❌ **tree-sitter 替代 plugins**(低 ROI, codegraph 兜符号层, plugins 是护城河)。
  - **拆开看 UA 的 `tree-sitter + LLM agent` 组合**: 确定性层**不做**(plugins+codegraph 已等价**且更深**——
    UA 的 tree-sitter 只到 AST 不建调用/跨层边, 我们有 callers/impact/cross-link/DI resolver);
    LLM agent 综合分析层**采纳 = 本轮 A1**, 但**不照抄 UA 管线**: UA 是 `AST→LLM`(grounding 弱),
    codev 是 `确定性图谱(impact report)→LLM`(grounding-first, 越界 reject, 信息密度高于裸 AST)。
  - **唯一例外(非本轮)**: 将来支持 plugins 未覆盖的语言(Go/Rust/C++)时, tree-sitter 的"广度开箱"
    才有真增量——届时是"快速拿符号级广度"的**补充**, 不是替换 plugins。本轮无此类仓, 有需求再说。
- ❌ **Java/Node/.NET call resolver**(违盲区原则)。
- ❌ **persona 三档画像系统**(过度设计;若做降为 `concise/verbose` 两档开关, 第一档喂 agent)。
- ❌ **language-concepts 高亮**(纯教学花架子, 与 codegraph 重叠)。
- ❌ **LLM 审 LLM 的 graph-reviewer**(用确定性 referential-integrity 校验替代, ROI 高)。
- ❌ **M6 Connector / M7 多模态**(缓)。
- ❌ **盘点"未做"不 Bash 直读就动手**(本会话 6 次文档滞后误判教训)。

---

## 七、设计哲学

技术血缘是**骨架**(plugins 产, 确定性, 不动), 综合理解是**血肉**(LLM analyzers 产, 软节点, 隔离),
在统一图谱上长在一起但**物理可分辨**(NodeKind + confidence + meta)。UA 是 "graphs that teach"(纯
LLM, human-first);codev 是 **"graphs that explain on a deterministic skeleton"** —— 确定性骨架
grounding LLM(比 UA 更抗幻觉), 产物服务 agent(AI-first), 多租户隔离(UA 没有)。

胜负手不在 UX, 在**业务域映射的准确(≥70% 可证伪)+ 可纠错(ownership 回路)+ 可被 agent/跨项目复用**。

---

## 八、四专家会诊收敛结论(2026-06-04, 沉淀)

| 共识 | 内容 |
|---|---|
| ROI 排序倒了 | C1→B1→A-MVP, A 不当 spearhead(收尾确定 > 新特性未验) |
| A 狠切 MVP | 只 A1 业务域只读文本, ≥70% 验收, 其余推下轮 |
| 软硬隔离 | 独立 kind + confidence<1.0 + impact 默认过滤软边 |
| grounding-first | LLM 当标注者不当发现者, 越界节点代码 reject(硬约束>prompt) |
| 缓存含版本 | fingerprint 并入 model+prompt_version, 按 flow 粒度失效 |
| 纠错回路 | ownership 一键纠正持久化 = PPT vs 真功能分水岭 |
| C1↔A 硬依赖 | C1 必须先(软节点要隔离列) |
| understand 只读 | LLM 只 ingest 跑一次, 查询零延迟 |
| 向量只换排序层 | redline-first 不变量前置保留 |
| AI-first 定位 | 不跟 UA 卷 tour/persona 视觉 |
